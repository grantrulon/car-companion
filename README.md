# car_companion

This is a Flutter personal learning project. The application uses Riverpod state management, Firebase for auth, Firestore for database, and Cloud Storage for file storage. It also has a basic integration with OpenAI API for an LLM chatbot response.

The application is meant to manage user car insurance, registration and maintenance records. 